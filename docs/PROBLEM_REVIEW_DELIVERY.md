# 资料问题二次复核交付

验收时间：2026-09-09 UTC（本机 2026-09-08）。

## 已启用

- 实施目录：`/Users/starryn/thinking/finwise-ontology-finance-system`。
- 本机工作台：<http://127.0.0.1:8767/static/operator.html>，后端已自动重启，PID 69305；前端资源版本 `problem-review-2`。
- 流程：首次筛选、脱敏证据整理、Agent 建议、确定性验证、版本绑定的复核展示。Agent 无权直接撤回提醒或执行财务操作。
- 复用现有 workbench、commands 和通用对象存储；包含持久化队列、单后台执行器、显式失败重试、失效检查及只读状态刷新。Gateway 启用时解析结果自动入队；关闭模型模式不产生自动模型任务。
- 页面保持原件优先，追加复核发现、关联原件版本与剩余缺口。后台完成只通知，不替换正在填写的页面。

## 验证结果

- Python 全量：793 项通过。
- Node 全量：121 项通过。
- 新增浏览器合成交互：9 条路径通过，包含请求失败保留草稿、明确请求、只读刷新、来源查看、关联红票版本、后台通知保留焦点。
- 本机真实页面只读验收：1440×900、1040×900、390×844 通过；进入详情、查看原件、刷新恢复，无业务写入及页面错误。
- 确定性红冲校验测试包含全额、多红票、部分红冲、金额差异、跨期、重复、来源失效、原值与规范化值不一致及版本失效。自动撤回能力已在合成证据完整的测试中验证，不能据此宣称当前真实两张蓝票已通过。

## 本期实际复核

聚贤达 2026-01 已显式执行一次存量复核，共 8 项；结果均为“依据不足”，撤回提醒 0 项：

- 6 项票据办理：尚需明确业务性质、实际业务期间及拟用科目，不以出票日猜测收票期间。
- 1 项发票状态任务，涉及 2 张蓝票：当前提取证据没有可核验的币种来源，尚不能认定客户缺资料，也不能默认人民币后撤回提醒。
- 1 项银行期间任务，涉及 2 条记录：原交易日期为 2026-02-02，当前办理期间为 2026-01，保留跨期判断事项。

这不是自动完成全部待办。币种证据的补齐或解析改造不在本轮范围内；未自动重解析或要求客户重新上传相同资料。

前后均为 229 条记录、219 条系统提取检查通过、2 条需核对、8 条期间异常；人工核实和账务可用均未增加。原件哈希、已有事实版本、人工处理记录及受保护页面校验一致。只新增复核及模型调用记录，不生成凭证、不确认期初。

## 模型用量

- 隔离副本两轮：16 次正式 DeepSeek 调用。
- 当前 Staging 一轮：8 次正式调用。
- 合计 24 次，返回用量共 13,665 tokens，模型 `deepseek-chat`，新阶段 `PROBLEM_REVIEW`。
- 调用元数据保留请求号、用量、延迟、模型/提示词/结构版本及输入输出哈希。供应商没有返回实际扣费金额，因此未把估算价格写成实际费用；实际扣费仍需供应商账单核对。
- 刷新和页面导航不触发新调用；人工核实、账务可用及原有模型覆盖统计未混算。

## 备份与证据

- 备份：`data/backups/pre-problem-review-2026-09-09T04-08-43+00-00/`（SQLite 一致性备份及附件副本）。
- 最终隔离复核：`output/problem-review/isolated-final.json`。
- 本机启用及数据保护报告：`output/problem-review/retained.json`。
- 真实浏览器报告：`output/playwright/source-first-retained/report.json`。
- 桌面截图：`output/playwright/source-first-retained/detail-1440.png`。
- 窄桌面截图：`output/playwright/source-first-retained/detail-1040.png`。
- 手机截图：`output/playwright/source-first-retained/detail-390.png`。

继续手测时刷新工作台，在“问题二次复核”查看现有结果即可，不必重复发起同版本复核。
